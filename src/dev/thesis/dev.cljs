(ns thesis.dev
  (:require [devtools.core :as devtools]
            ;[dirac.runtime :as dirac]
            ))

; -------------------------------------------------------------------------------------------------------------------
; has to be included before boot

(devtools/install!)
;(dirac/install!)
